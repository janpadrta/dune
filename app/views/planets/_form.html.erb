<%= form_for(planet) do |f| %>
  <% if planet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planet.errors.count, "error") %> prohibited this planet from being saved:</h2>

      <ul>
      <% planet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :system_id %>
    <%= f.number_field :system_id %>
  </div>

  <div class="field">
    <%= f.label :position %>
    <%= f.number_field :position %>
  </div>

  <div class="field">
    <%= f.label :discovered_at %>
    <%= f.datetime_select :discovered_at %>
  </div>

  <div class="field">
    <%= f.label :house_id %>
    <%= f.number_field :house_id %>
  </div>

  <div class="field">
    <%= f.label :available_to_all %>
    <%= f.check_box :available_to_all %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
